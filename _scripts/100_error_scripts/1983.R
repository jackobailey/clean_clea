
# Panama (May, 1999)

# For some reason the data in this election are duplicated such that each row
# appears twice. This is solved (in general) by removing duplicated rows.

# dta |>
#   filter(id == 1983) |>
#   nrow()

# dta |>
#   filter(id == 1983) |>
#   distinct() |>
#   nrow()
